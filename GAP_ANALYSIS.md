# AI 智能测试工作台：PRD 与实现差距分析

本文档旨在指出当前项目实现与所提供的 `PRD.md`（智能测试工作台系统设计方案）之间存在的差异和缺失功能。

## 概要总结

当前实现成功搭建了 AI 智能测试工作台的基础架构，包括主用户界面布局（实时预览、步骤列表、代码编辑器）、双模式脚本生成（静态/Playwright vs. 动态/AI），以及核心的 AI 意图到步骤的转译功能。

然而，PRD 中规定的几个关键、高影响力的功能要么完全缺失，要么实现得非常表面。最显著的差距在于：缺乏一个功能性的**公共库（Shared Library）**用于复用测试逻辑，一个尚不完善的**交互式录制器（Interactive Recorder）**，以及**选择器优化（Selector Refinement）**和**调试快照（Debug Snapshots）**功能的缺失。现有功能仅能作为一个概念验证（Proof-of-Concept），尚未构成设计文档中所描述的“低代码、高智能、自愈合”平台。

## 高影响力缺失功能

这些功能是产品愿景的核心，它们的缺失代表了与 PRD 的重大偏离。

### 1. **公共库功能 (PRD 第 3.E 节)**
*   **缺失**: 选中多个步骤并**提取为可复用组件**的能力。
*   **缺失**: 对这些提取的组件进行**参数化**的能力（例如，将硬编码的用户名转为函数参数）。
*   **缺失**: 当共享组件被修改时，**更新所有引用该组件的测试用例**的逻辑。
*   **当前状态**: `ComponentLibrary` 只是一个占位符，仅允许添加预定义的静态模板，不支持用户创建可复用组件。

### 2. **交互式录制功能增强 (PRD 第 3.C 节)**
*   **缺失**: **元素拾取器与操作菜单**。PRD 描述了一个工作流，用户可以在实时预览中通过快捷键（如 Cmd+Click）点击一个元素，然后弹出一个包含上下文感知操作（如“点击”、“输入”、“断言文本”）的菜单。
*   **缺失**: **AI 增强建议**。PRD 规定，操作菜单应通过 AI 建议得到增强（例如，点击一个价格标签会建议“验证价格大于 0”）。
*   **当前状态**: 录制功能仅限于一个简单的 `onClick` 事件，该事件生成一个“点击”步骤，没有其他选项或智能功能。

### 3. **智能选择器优化 (PRD 第 3.D 节)**
*   **缺失**: 完整的**选择器优化管道**。这包括：
    *   一个用于移除选择器中不稳定、动态部分的“清洗管道”。
    *   基于元素上下文对变量进行语义化重命名。
    *   生成多维定位器（例如，结合 CSS、XPath 和文本）以提高脚本的健壮性。
*   **当前状态**: 系统似乎直接使用了 AI 服务返回的 `precise` 选择器，客户端没有应用任何进一步的优化或回退策略。

### 4. **步骤级回放快照 (PRD 第 3.F 节)**
*   **缺失**: 每执行一步后自动截取 **DOM 和屏幕快照**的功能。
*   **当前状态**: 存在一个模拟的 `handleRunStep` 函数，但它只模拟了延迟和状态变更。用于追踪失败的关键调试功能——快照对比，尚未实现。

## 中等影响力缺失功能

这些功能对于提升可用性和实现“低代码”承诺非常重要。

### 1. **模板引擎自动补全 (PRD 第 3.B 节)**
*   **缺失**: **参数自动补全机制**。例如，当添加一个“表单输入”模板时，系统应在实时预览窗口中高亮所有 `<input>` 元素，允许用户点击其中一个来自动填充其选择器。
*   **当前状态**: 添加模板时带有占位符数据，需要用户手动查找并输入选择器信息。

### 2. **步骤级 AI 策略覆盖**
*   **缺失**: 在 `types.ts` 中定义的 `Step` 数据结构中，缺少 `config: { forceAI: false }` 字段。
*   **影响**: 这使得用户无法为单个步骤覆盖全局脚本生成策略，降低了灵活性。

## 低影响力差异与次要差距

1.  **不完整的导出功能**: 头部的“导出脚本”按钮虽然存在，但没有绑定任何用于下载生成代码文件的逻辑。
2.  **选择器工厂策略**: PRD 指定了选择选择器的明确优先级（ID > 语义等）。该逻辑未在前端明确实现；系统完全依赖 AI 的输出。
3.  **模拟的回放**: 步骤级回放是模拟的，并不会真正在 `BrowserPreview` 中执行步骤的操作。

## 结论与建议

开发团队应优先处理**高影响力缺失功能**，以使产品与核心愿景保持一致。建议的实施顺序如下：

1.  **完善交互式录制器**，增加操作菜单和 AI 建议，因为这是用户的主要输入方式。
2.  **实现核心的公共库功能**（提取和复用），这对构建可扩展的测试套件至关重要。
3.  **构建选择器优化管道**，以提高生成的静态脚本的可靠性。
4.  **在回放机制中添加快照对比功能**，以提供必要的调试能力。

解决这些差距将使项目从一个有前景的原型，转变为一个强大而实用的测试工具。